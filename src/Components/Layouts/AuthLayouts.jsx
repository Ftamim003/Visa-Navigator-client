import { Outlet } from "react-router-dom";



const AuthLayouts = () => {
    return (
        <div>
            <header>
               
            </header>
            <Outlet></Outlet>
        </div>
    );
};

export default AuthLayouts;